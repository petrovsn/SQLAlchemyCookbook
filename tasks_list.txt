Модуль 1. Сессия и транзакции (1–2 дня)
Задача 1.1. Поймать DetachedInstanceError
Что сделать: Напишите функцию, которая получает объект, закрывает сессию и пытается прочитать его атрибут.

Исследовать:

Что такое expire_on_commit?

Как session.expire() и session.refresh() влияют на объект?

Как сделать объект "безопасным" для передачи в другие слои?

✅ Успех: Можете объяснить 3 способа передать данные из сессии наружу без ошибок.
(dto, joinedload, session.expunge(user))

Задача 1.2. Воспроизвести гонку с rollback() внутри цикла
Что сделать: Напишите функцию, которая вставляет 5 пользователей, при ошибке на 4-м вызывает rollback().

Исследовать:

Что произойдёт с первыми 3-мя?

Что будет с объектами в сессии после rollback()?

Как исправить через begin_nested()?

✅ Успех: Можете объяснить разницу между rollback() всей транзакции и откатом savepoint.

Задача 1.3. Сравнить поведение commit() и flush() с expire_on_commit
Что сделать: Напишите скрипт, который:

Загружает объект.

Меняет его.

Вызывает flush() — читает поля.

Вызывает commit() — снова читает поля.

Исследовать:

Сколько SELECT-ов выполнилось?

Что делает expire_on_commit?

Как поведёт себя session.get() после коммита?

✅ Успех: Можете предсказать количество запросов в разных сценариях.

Задача 1.4. Утечка соединений
Что сделать: Напишите код, который создаёт 1000 сессий без close() и смотрит, когда упадёт пул соединений.

Исследовать:

Параметры пула: pool_size, max_overflow, pool_recycle.

Как with Session() гарантирует закрытие?

Что делает session.commit() с соединением?
Оставляет открытым, можно продолжать работать, но 
все объекты будут помечены как expired 

✅ Успех: Можете объяснить, почему session.commit() без close() — это проблема.

Модуль 2. Загрузка данных и N+1 (2–3 дня)
Задача 2.1. Воспроизвести N+1
Что сделать: Создайте 10 пользователей, у каждого по 5 постов. Загрузите пользователей и в цикле прочитайте их посты. Включите echo=True и посчитайте запросы.

Исследовать:

Почему запросов 11, а не 1?

Где в логах видно проблему?

✅ Успех: Увидели N+1 своими глазами и можете объяснить, почему это плохо.

Задача 2.2. Три стратегии загрузки
Что сделать: Для того же сценария примените по очереди:

joinedload()

subqueryload()

selectinload()

Исследовать:

Сколько запросов в каждом случае?

Какой из них самый быстрый для коллекций? Для одиночных объектов?

Сравните EXPLAIN каждого запроса.

✅ Успех: Можете выбрать стратегию для конкретного кейса и обосновать.

Задача 2.3. selectinload() и много ID
Что сделать: Возьмите 1000 пользователей и примените selectinload() к их постам.

Исследовать:

Как выглядит сгенерированный SQL?

Что произойдёт, если ID будет 5000?

Как ограничить количество ID в одном запросе? (подсказка: selectinload() сам это делает через chunk_size)

✅ Успех: Знаете, что selectinload() чанкует запросы и как это контролировать.

Задача 2.4. Ленивая загрузка внутри цикла — удар по производительности
Что сделать: Загрузите 100 пользователей с ленивой загрузкой постов, внутри цикла обратитесь к user.posts.

Исследовать:

Сколько времени это занимает (замерить timeit).

Как это исправить через eager load.

Почему в асинхронном режиме такая ошибка приведёт к падению?
Потому что при lazy load по умолчанию будет синхронный код в асинк-контексте

✅ Успех: Можете объяснить, почему "ленивая загрузка — это зло в API-эндпоинтах".

Модуль 3. Массовые операции и оптимизация (2 дня)
Задача 3.1. add_all() vs bulk_insert_mappings() vs INSERT ... VALUES
Что сделать: Вставьте 10000 записей тремя способами и замерьте время.

Исследовать:

Почему add_all() медленный?
Потому что там десятки и сотни отдельных инсертов

Что теряет bulk_insert_mappings() (отключение ORM-событий, каскадов, валидации)?

Когда использовать insert() с executemany()?
Всегда когда есть много одинаковых записей. bulk_insert_mappings сейчас считается legacy

✅ Успех: Можете выбрать метод для пакетной загрузки с обоснованием.

Задача 3.2. Массовое обновление с синхронизацией сессии
Что сделать: Выполните:

python
session.query(User).filter(User.active == True).update({"status": "inactive"})
Исследовать:

Что произойдёт с объектами уже загруженными в сессию?

Параметр synchronize_session='fetch' vs 'evaluate' — в чём разница?

Когда нужно использовать expire_all() после обновления?

✅ Успех: Знаете, что массовый update() — это "бомба" для отслеживаемых объектов.

Задача 3.3. ON CONFLICT (Upsert) в PostgreSQL
Что сделать: Реализуйте вставку или обновление по уникальному ключу без гонок.

Исследовать:

on_conflict_do_update() vs do_nothing().

Как это сделать в MySQL (ON DUPLICATE KEY UPDATE)?

✅ Успех: Можете объяснить, почему это лучше, чем SELECT + INSERT вручную.

Модуль 4. Отношения и каскады (2 дня)
Задача 4.1. Каскадное удаление
Что сделать: Создайте User → Post (один ко многим). Удалите пользователя и посмотрите, что произошло с постами.

Исследовать:

Каскады: delete, delete-orphan, all.

Что произойдёт, если FK без ON DELETE CASCADE?

Разница между каскадом в БД и каскадом в ORM.

✅ Успех: Можете выбрать правильный каскад для User → Post и Order → OrderItem.

Задача 4.2. Проблема с back_populates и двусторонними связями
Что сделать: Создайте две модели с relationship и back_populates. Добавьте объект с одной стороны, прочитайте с другой.

Исследовать:

Почему объект появляется с другой стороны только после flush()?

Что делает backref и чем отличается от back_populates?

✅ Успех: Можете объяснить, почему при добавлении Post к User.posts нужно явно установить post.user = user.

Задача 4.3. secondary таблица — многие ко многим
Что сделать: Реализуйте User → Group через secondary. Добавьте пользователя в группу, удалите группу.

Исследовать:

Как удаляется связь из ассоциативной таблицы?

Что такое ассоциативный объект (с дополнительными полями)?

Когда использовать secondary vs явную модель для связи?

✅ Успех: Можете реализовать связь "многие ко многим" с дополнительным полем (например, role в членстве).

Модуль 5. Асинхронный SQLAlchemy (2 дня)
Задача 5.1. Миграция синхронного кода в асинхронный
Что сделать: Возьмите простой CRUD на синхронной сессии и перепишите на AsyncSession.

Исследовать:

Как настраивается create_async_engine?

Какие методы становятся await?

Почему нельзя использовать lazy='select' в асинхронном режиме?

✅ Успех: Работающий асинхронный эндпоинт без блокировок event loop.

Задача 5.2. Асинхронная сессия на всю жизнь запроса
Что сделать: Напишите middleware для FastAPI, который создаёт и закрывает сессию на каждый запрос.

Исследовать:

Как использовать async_sessionmaker?

Как передать сессию в зависимости (Dependency Injection)?

Почему нельзя использовать одну сессию для нескольких корутин?

✅ Успех: Есть рабочий middleware и понимание скоупа сессии.

Задача 5.3. Конкурентные запросы с asyncio.gather()
Что сделать: Выполните 10 параллельных запросов к БД через gather() с одной сессией.

Исследовать:

Упадёт ли код? (спойлер: да, сессия не потокобезопасна)

Как исправить — создавать сессию на каждый запрос или использовать пул?

✅ Успех: Понимаете, что AsyncSession не разделяется между корутинами.

Модуль 6. Миграции и Alembic (1 день)
Задача 6.1. Переименование колонки через Alembic
Что сделать: Переименуйте поле в модели, сгенерируйте миграцию и исправьте её вручную.

Исследовать:

Почему autogenerate создаёт ADD + DROP, а не RENAME?

Как написать правильную миграцию с alter_column()?

Как откатить миграцию (downgrade)?

✅ Успех: Есть миграция, которая переименовывает колонку без потери данных.

Задача 6.2. Добавление NOT NULL с дефолтом
Что сделать: Добавьте новое поле created_at с nullable=False и default=func.now() в существующую таблицу.

Исследовать:

В каком порядке Alembic выполняет операции?

Почему нужно добавить поле с nullable=True, заполнить, потом сделать nullable=False?

Как это сделать в одной миграции?

✅ Успех: Можете объяснить пошаговый план миграции для больших таблиц.

Модуль 7. Профилирование и отладка (1 день)
Задача 7.1. Включить логирование всех запросов
Что сделать: Настройте echo=True и logging для SQLAlchemy, чтобы видеть все запросы с параметрами.

Исследовать:

Как отфильтровать только медленные запросы?

Как логировать время выполнения каждого запроса?

Инструменты: cProfile, SQLAlchemy-Continuum, slow-query-log.

✅ Успех: Настроили логирование и можете найти самый тяжёлый запрос в проекте.

Задача 7.2. EXPLAIN ANALYZE для ORM-запросов
Что сделать: Возьмите сложный запрос с JOIN-ами и получите его план выполнения.

Исследовать:

Как получить SQL из select()? (str(query))

Как выполнить EXPLAIN ANALYZE через session.execute(text(...))?

Как читать план и находить проблемные места (Seq Scan, Index Scan, Nested Loop)?

✅ Успех: Можете оптимизировать запрос, глядя на EXPLAIN.

Финальный проект (1 день)
Задача 8. Мини-приложение с нагрузкой
Что сделать: Напишите REST API (FastAPI/Flask) с:

Получением списка пользователей с пагинацией и их постами (оптимизировать N+1).

Массовым импортом 1000 пользователей через bulk_insert или ON CONFLICT.

Асинхронным эндпоинтом, который параллельно запрашивает данные из 3 таблиц.

Миграцией, которая добавляет новое поле в существующую таблицу.

Исследовать:

Все пункты выше в одном проекте.

Замерить производительность до и после оптимизаций.

Написать тесты для каждой фичи.

✅ Успех: Работающее приложение, которое не падает под нагрузкой.

Итоговый чек-лист для собеседования
После решения всех задач вы сможете ответить на вопросы:

□ Что такое Identity Map и как он влияет на объекты?
□ В чём разница между flush() и commit()?
□ Как работает expire_on_commit и когда его выключать?
□ Что такое N+1 и как исправить?
□ Когда использовать joinedload, selectinload, subqueryload?
□ Почему rollback() внутри цикла — это плохо?
□ Как вставить 100000 записей без падения производительности?
□ Чем опасен массовый update() для объектов в памяти?
□ Как настроить асинхронную сессию и почему её нельзя шарить между корутинами?
□ Как написать миграцию с переименованием колонки без потери данных?